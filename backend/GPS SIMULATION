import requests
import time

url = "http://localhost:5000/location"

data = {
    "vehicle_id": "vehicle_1",
    "latitude": 12.9716,
    "longitude": 77.5946
}

while True:
    res = requests.post(url, json=data)
    print(res.json())
    time.sleep(5)
